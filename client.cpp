#include "employee.h"
#include <iostream>
#include <Windows.h>

int main() {
    while (true)
    {
        std::string whatToDo;
        std::cout << "modify, read or exit(m,r,e): ";
        if (whatToDo == "m") {
            
        }
    }
    
}